apiversion: rbac.authorization.k8s.io/v1
kind: Admin-role
metadata:
  name: roboshop-admin
  namespace: roboshop
rules:
 - apiGroup: ["*"] # "" means the core API group
   resources: ["*"]
   verbs: ["*"] 