apiversion: v1
kind: pod
metadata:
    name: aws-cli
    namespace: roboshop
spec:
    servicesAccount: roboshop-reader
    containers:
      - name: aws-cli
        image: amazon/aws-cli
        command: ["sleep" , "1000"]